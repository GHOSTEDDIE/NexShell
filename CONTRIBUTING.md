# 贡献指南

先运行现有测试，再修改模块。SSH、文件、监控服务应由人工界面和 Agent 复用；新增 AI 工具接入统一授权与执行记录，明确绑定主机编号。

修改算法、权限、传输或终端逻辑时增加能够暴露缺陷的回归用例，并保留原有正确样本。测试环境限定为独立容器或明确指定的测试服务器。

桌面回调中只更新界面，网络和模型操作放在后台；跨线程界面更新使用 `fyne.Do`。界面文案面向运维操作，架构解释放到文档。

提交时说明具体行为、验证命令与未覆盖的平台。依赖升级保留许可证，终端依赖升级必须同时运行上游测试及应用层兼容测试。
