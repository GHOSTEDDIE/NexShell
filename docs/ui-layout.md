# 工作台布局与主题

2026-09-08 只读查看本机 FinalShell 4.5.12：窄侧栏、顶部会话标签、主终端、下方文件面板。参考其信息层级，保留 NexShell 的连接列表与上下终端／文件工作区，精简顶部操作，运维助手通过顶部开关按需展开。

右上角提供亮色、暗色选择；首次启动默认亮色，选择保存为应用偏好，下次启动恢复。终端默认前景、背景、选区随主题更新，远端显式指定的字符颜色保留。导入、导出、模型设置、快捷命令、删除连接集中在“更多”。

验证：

- `go test -race -tags ci ./...` 通过，复跑中文、组合字符、历史与备用屏幕等原有终端样本。
- 新增主题独立于系统设置、偏好恢复、终端切换主题保留内容的回归。
- `go vet -tags ci ./...` 通过。
- 本机 macOS 应用实际查看亮色、切换暗色、展开及收起运维助手。
- 已更新 `dist/NexShell.app` 与 macOS arm64 压缩包；其他平台安装包尚未包含本次界面改动。

本次未连接用户生产服务器，未将 FinalShell 的连接资料导入项目。已连接工作区的完整视觉验收仍需隔离服务器场景。
